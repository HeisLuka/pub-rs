# Fixtures

Do not add arbitrary large PUB corpora here.

Small regression fixtures may be committed when redistribution is appropriate and they prove a specific behavior. Record origin, SHA-256, format family and purpose.

Large corpora should be fetched only by explicit/manual workflows and verified by hash.
